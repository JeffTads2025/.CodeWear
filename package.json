{
    "name": "codewear-project",
    "private": true,
    "scripts": {
        "prepare": "husky install",
        "lint": "cd Frontend-CodeWear && npm run build",
        "test:e2e": "cd Backend-CodeWear && npm test -- --runInBand",
        "test:ci": "npm run lint && npm run test:e2e"
    },
    "devDependencies": {
        "@commitlint/cli": "^19.1.0",
        "@commitlint/config-conventional": "^19.1.0",
        "husky": "^9.0.1"
    }
}